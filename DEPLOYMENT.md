# Инструкция по деплою на Render.com

## ⚠️ Важно: Ollama на Render.com

### Варианты решения:

1. **🐳 Docker с Ollama** (НОВОЕ - рекомендуется если есть Standard план)
   - Используйте Dockerfile для упаковки Ollama + приложения
   - Требует **Standard план** или выше (минимум 2GB RAM)
   - См. [DOCKER_DEPLOYMENT.md](./DOCKER_DEPLOYMENT.md) для деталей

2. **Использовать внешний Ollama сервер** (рекомендуется для бесплатного плана)
   - Запустить Ollama на отдельном сервере (VPS, домашний компьютер)
   - Указать URL в переменной `OLLAMA_BASE_URL`

3. **Использовать облачный Ollama сервис**
   - Например, через API провайдеров

4. **Отключить LLM функционал** (временно)
   - Бот будет работать без ИИ ответов в группе

## 📋 Шаг 1: Подготовка репозитория

1. Убедитесь, что все изменения закоммичены:
```bash
git add .
git commit -m "Prepare for deployment"
git push origin main
```

2. Проверьте, что `.gitignore` содержит:
```
.env
data/
venv/
*.db
*.sqlite
```

## 📋 Шаг 2: Создание сервиса на Render.com

### 2.1. Создание Web Service

1. Зайдите на [Render.com](https://render.com)
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройте:
   - **Name:** `wedding-bot` (или любое другое)
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python server.py`
   - **Plan:** Free или Starter (рекомендуется Starter для стабильности)

### 2.2. Настройка переменных окружения

В разделе "Environment" добавьте следующие переменные:

#### Обязательные переменные:

```bash
# Telegram Bot
BOT_TOKEN=your_bot_token_here

# Свадьба
WEDDING_DATE=2026-06-05
GROOM_NAME=Валентин
BRIDE_NAME=Мария
WEDDING_ADDRESS=Разъезжая улица, 15, городской посёлок Токсово, Токсовское городское поселение, Всеволожский район, Ленинградская область

# Mini App
WEBAPP_URL=https://your-app-name.onrender.com

# Группа
GROUP_LINK=https://t.me/+your_group_link
GROUP_ID=your_group_id_here

# Админы (через запятую, с @)
ADMINS=@username1,@username2

# Google Sheets
GOOGLE_SHEETS_ID=your_google_sheets_id
GOOGLE_SHEETS_CREDENTIALS={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}

# Ollama (если используете внешний сервер)
OLLAMA_BASE_URL=http://your-ollama-server:11434
OLLAMA_MODEL=qwen2.5:7b
```

#### Опциональные переменные:

```bash
# Telegram аккаунты
GROOM_TELEGRAM=ezhigval
BRIDE_TELEGRAM=mrfilmpro

# Старый способ админов (если не используете Google Sheets)
ADMIN_USER_ID=your_telegram_user_id
```

### 2.3. Важные настройки Render

1. **Auto-Deploy:** Включите для автоматического деплоя при push в main
2. **Health Check Path:** `/` (опционально)
3. **Port:** Render автоматически устанавливает порт через переменную `PORT`

## 📋 Шаг 3: Настройка Ollama (если используете)

### Вариант A: Внешний сервер (VPS/домашний компьютер)

1. Установите Ollama на сервере:
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

2. Запустите Ollama:
```bash
ollama serve
```

3. Установите модель:
```bash
ollama pull qwen2.5:7b
```

4. Убедитесь, что сервер доступен из интернета:
   - Откройте порт 11434 в firewall
   - Если используете домашний компьютер, настройте port forwarding или используйте ngrok

5. Укажите URL в `OLLAMA_BASE_URL` на Render

### Вариант B: Использование ngrok для локального Ollama

Если Ollama запущен локально:

```bash
# Установите ngrok
# Запустите туннель
ngrok http 11434

# Используйте URL от ngrok в OLLAMA_BASE_URL
# Например: https://abc123.ngrok.io
```

⚠️ **Внимание:** Бесплатный ngrok меняет URL при каждом перезапуске!

## 📋 Шаг 4: Настройка Google Sheets

1. Создайте Service Account в Google Cloud Console:
   - Перейдите в [Google Cloud Console](https://console.cloud.google.com)
   - Создайте проект или выберите существующий
   - Включите Google Sheets API
   - Создайте Service Account
   - Скачайте JSON ключ

2. Предоставьте доступ к таблице:
   - Откройте вашу Google таблицу
   - Нажмите "Поделиться"
   - Добавьте email из Service Account (находится в JSON)
   - Дайте права "Редактор"

3. Добавьте JSON в переменную `GOOGLE_SHEETS_CREDENTIALS`:
   - Скопируйте весь JSON одной строкой
   - Вставьте в переменную окружения на Render

## 📋 Шаг 5: Проверка деплоя

1. После деплоя проверьте логи:
   - В Render Dashboard → Logs
   - Убедитесь, что нет ошибок

2. Проверьте работу бота:
   - Отправьте `/start` боту
   - Проверьте, что приходит приглашение

3. Проверьте Mini App:
   - Откройте `https://your-app-name.onrender.com`
   - Убедитесь, что сайт загружается

4. Проверьте работу LLM (если настроен):
   - Напишите в группу `@бот когда свадьба?`
   - Проверьте, что бот отвечает

## 📋 Шаг 6: Настройка базы данных

База данных SQLite создается автоматически в папке `data/`.

⚠️ **Важно:** На бесплатном плане Render файловая система эфемерная (стирается при перезапуске).

### Решения:

1. **Использовать PostgreSQL** (рекомендуется для продакшена)
   - Создайте PostgreSQL на Render
   - Обновите код для использования PostgreSQL вместо SQLite

2. **Использовать Google Sheets** как основное хранилище (уже реализовано)

3. **Использовать внешнее хранилище** (S3, etc.)

## 🔧 Устранение проблем

### Бот не отвечает

1. Проверьте `BOT_TOKEN` в переменных окружения
2. Проверьте логи на Render
3. Убедитесь, что бот запущен (проверьте логи)

### LLM не работает

1. Проверьте `OLLAMA_BASE_URL` - должен быть доступен из интернета
2. Проверьте, что Ollama запущен и модель установлена
3. Проверьте логи на наличие ошибок подключения

### Google Sheets не работает

1. Проверьте `GOOGLE_SHEETS_CREDENTIALS` - должен быть валидный JSON
2. Проверьте, что Service Account имеет доступ к таблице
3. Проверьте `GOOGLE_SHEETS_ID` - должен быть правильный ID таблицы

### Mini App не загружается

1. Проверьте `WEBAPP_URL` - должен совпадать с URL на Render
2. Проверьте, что файлы в папке `webapp/` присутствуют
3. Проверьте логи на наличие ошибок

## 📝 Чек-лист перед деплоем

- [ ] Все изменения закоммичены и запушены
- [ ] `.gitignore` настроен правильно
- [ ] Все переменные окружения настроены на Render
- [ ] Ollama настроен (если используется)
- [ ] Google Sheets настроен
- [ ] `WEBAPP_URL` указывает на правильный домен
- [ ] `GROUP_ID` настроен
- [ ] Админы добавлены в `ADMINS`

## 🚀 После деплоя

1. Проверьте работу всех функций:
   - Регистрация гостей
   - Отправка приглашений
   - Работа в группе
   - Команды админов
   - LLM ответы (если настроен)

2. Настройте мониторинг (опционально):
   - Используйте Health Check на Render
   - Настройте уведомления об ошибках

3. Регулярно проверяйте логи на наличие ошибок

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи на Render
2. Проверьте все переменные окружения
3. Убедитесь, что все сервисы доступны

