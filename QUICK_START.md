# ⚡ Быстрый старт - пошаговая инструкция

## 🎯 Цель: Запустить бота и разместить его на сервере

---

## ШАГ 1: Получить токен бота (5 минут)

1. Откройте Telegram
2. Найдите `@BotFather` (официальный бот Telegram)
3. Отправьте `/start`
4. Отправьте `/newbot`
5. Придумайте имя: `Свадьба Валентин и Мария`
6. Придумайте username (должен заканчиваться на `bot`): `wedding_val_mar_bot`
7. **Скопируйте токен**, который даст BotFather (выглядит как: `1234567890:ABC...`)

📖 **Подробная инструкция:** см. файл `GET_TOKEN.md`

---

## ШАГ 2: Настроить проект локально (2 минуты)

1. Создайте файл `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` и вставьте ваш токен:
   ```
   BOT_TOKEN=ваш_токен_здесь
   WEDDING_DATE=2026-06-06
   GROOM_NAME=Валентин
   BRIDE_NAME=Мария
   ```

3. Протестируйте бота локально:
   ```bash
   source venv/bin/activate
   python bot.py
   ```

4. Найдите вашего бота в Telegram и отправьте `/start`
5. Если всё работает - нажмите `Ctrl+C` чтобы остановить

---

## ШАГ 3: Разместить на сервере (10 минут) - РЕКОМЕНДУЕТСЯ: Railway

### 3.1 Создать GitHub репозиторий

1. Зайдите на [github.com](https://github.com)
2. Зарегистрируйтесь (если еще нет аккаунта)
3. Нажмите "New repository"
4. Название: `wedding-bot`
5. Выберите "Private" (чтобы токен не был публичным)
6. Нажмите "Create repository"

### 3.2 Загрузить проект в GitHub

**Вариант A: Через веб-интерфейс GitHub**
- Просто перетащите все файлы (кроме `venv/`, `.env`, `data/`) в браузер

**Вариант B: Через командную строку** (если установлен git):
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш_username/wedding-bot.git
git push -u origin main
```

### 3.3 Разместить на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "Login" → выберите "Login with GitHub"
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий `wedding-bot`
6. Railway начнет автоматическую установку

7. **Добавьте переменные окружения:**
   - Нажмите на проект
   - Перейдите в "Variables"
   - Добавьте:
     - `BOT_TOKEN` = ваш токен
     - `WEDDING_DATE` = 2026-06-06
     - `GROOM_NAME` = Валентин
     - `BRIDE_NAME` = Мария

8. Railway автоматически перезапустит бота с новыми переменными

9. **Проверьте логи:**
   - В разделе "Deployments" нажмите на последний деплой
   - Посмотрите логи - должны быть сообщения "Бот запущен"

### 3.4 Готово! ✅

Бот теперь работает 24/7! Найдите его в Telegram и протестируйте.

---

## 🆘 Проблемы?

### Бот не отвечает:
- Проверьте логи в Railway
- Убедитесь, что токен правильный
- Проверьте, что все переменные окружения добавлены

### Ошибка при деплое:
- Убедитесь, что файл `requirements.txt` есть в репозитории
- Проверьте, что `bot.py` в корне проекта

### Нужна помощь?
- Смотрите `DEPLOY.md` для других вариантов размещения
- Или напишите мне - помогу!

---

## 📱 Что дальше?

1. ✅ Бот работает 24/7
2. ✅ Отправьте гостям ссылку на бота: `https://t.me/ваш_бот_username`
3. ✅ Гости могут писать `/start` и регистрироваться
4. ✅ Вы можете смотреть список гостей командой `/guests`

**Поздравляю! Ваш свадебный бот готов! 🎉**

