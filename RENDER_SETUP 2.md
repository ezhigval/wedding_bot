# Быстрая настройка на Render.com

## 🚀 Быстрый старт

### 1. Подготовка репозитория

```bash
# Убедитесь, что все закоммичено
git add .
git commit -m "Ready for deployment"
git push origin main
```

### 2. Создание сервиса на Render

1. Зайдите на [dashboard.render.com](https://dashboard.render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите GitHub репозиторий
4. Настройте:
   - **Name:** `wedding-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python server.py`

### 3. Переменные окружения

В разделе **"Environment"** добавьте переменные из `.env.example`:

#### Минимальный набор:

```bash
BOT_TOKEN=ваш_токен_бота
WEBAPP_URL=https://wedding-bot-xxxx.onrender.com
GROUP_ID=id_вашей_группы
ADMINS=@ваш_username
GOOGLE_SHEETS_ID=id_таблицы
GOOGLE_SHEETS_CREDENTIALS={"type":"service_account",...}
```

#### Для работы LLM:

```bash
OLLAMA_BASE_URL=http://ваш-ollama-сервер:11434
OLLAMA_MODEL=qwen2.5:7b
```

### 4. Настройка Ollama (варианты)

#### Вариант 1: Внешний VPS
- Установите Ollama на VPS
- Откройте порт 11434
- Укажите IP в `OLLAMA_BASE_URL`

#### Вариант 2: Локальный компьютер + ngrok
```bash
# Установите ngrok
# Запустите туннель
ngrok http 11434

# Используйте URL от ngrok
OLLAMA_BASE_URL=https://xxxx.ngrok.io
```

#### Вариант 3: Без LLM
- Не указывайте `OLLAMA_BASE_URL`
- Бот будет работать без ИИ ответов

### 5. Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя
3. Проверьте логи на наличие ошибок

### 6. Проверка

- Откройте `https://your-app.onrender.com` - должен загрузиться сайт
- Отправьте `/start` боту - должен прийти ответ
- Проверьте работу в группе

## ⚠️ Важные моменты

1. **Ollama на Render:** Не работает напрямую, нужен внешний сервер
2. **База данных:** SQLite стирается при перезапуске на бесплатном плане
3. **Переменные:** Все чувствительные данные только через Environment Variables
4. **Логи:** Всегда проверяйте логи при проблемах

## 🔧 Частые проблемы

**Бот не отвечает:**
- Проверьте `BOT_TOKEN`
- Проверьте логи

**LLM не работает:**
- Проверьте доступность `OLLAMA_BASE_URL`
- Проверьте, что Ollama запущен

**Google Sheets не работает:**
- Проверьте `GOOGLE_SHEETS_CREDENTIALS` (должен быть валидный JSON)
- Проверьте доступ Service Account к таблице

## 📚 Подробная инструкция

См. [DEPLOYMENT.md](./DEPLOYMENT.md) для детальной инструкции.

