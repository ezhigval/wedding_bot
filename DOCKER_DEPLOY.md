# Деплой в Docker на Render.com

## Быстрый старт

1. **Создайте новый Docker сервис на Render.com**
   - Выберите "New" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите ветку `main`
   - **Важно:** Выберите "Docker" как Environment (не Python!)

2. **Настройте переменные окружения**
   - `BOT_TOKEN` - токен Telegram бота
   - `GOOGLE_SHEETS_ID` - ID Google таблицы
   - `GOOGLE_CREDENTIALS_JSON` - JSON с credentials для Google Sheets
   - `PORT` - порт (обычно 10000, Render установит автоматически)

3. **Запустите деплой**
   - Render автоматически соберет Docker образ
   - Запустит контейнер с ботом и веб-сервером

## Структура

- `Dockerfile` - определение Docker образа
- `start.sh` - скрипт запуска приложения
- `.dockerignore` - файлы, исключенные из образа

## Проверка

После деплоя проверьте логи на Render:
- Должно быть: `✅ Переменные окружения проверены`
- Должно быть: `🌐 Веб-сервер запущен на порту 10000`
- Должно быть: `🤖 Запуск бота (polling)...`

## Проблемы

Если бот не запускается:
1. Проверьте переменные окружения
2. Проверьте логи на Render
3. Убедитесь, что используется Docker environment (не Python)

