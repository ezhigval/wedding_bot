# 🔧 Исправление ошибки деплоя на Render

## ❌ Ошибка:
```
TokenValidationError: Token is invalid! It can't contains spaces.
```

## ✅ Решение:

### Проблема:
Токен бота содержит пробелы или неправильно указан в переменных окружения на Render.

### Как исправить:

1. **Зайдите на Render:**
   - Откройте ваш сервис `wedding-bot`
   - Перейдите в **Environment**

2. **Проверьте переменную BOT_TOKEN:**
   - Найдите `BOT_TOKEN`
   - Нажмите на неё для редактирования

3. **Убедитесь, что значение ТОЧНО такое (БЕЗ пробелов и кавычек):**
   ```
   8295772557:AAH_udSU1FyjYH-mekFDZht2Yuy0EKI7s5g
   ```

4. **Важно:**
   - ❌ НЕ должно быть пробелов в начале
   - ❌ НЕ должно быть пробелов в конце
   - ❌ НЕ должно быть кавычек (ни одинарных, ни двойных)
   - ✅ Только сам токен

5. **Сохраните изменения:**
   - Нажмите **Save Changes**
   - Render автоматически перезапустит сервис

6. **Проверьте другие переменные:**
   Убедитесь, что все переменные без пробелов:
   - `WEDDING_DATE` = `2026-06-06` (без пробелов)
   - `GROOM_NAME` = `Валентин` (без пробелов в начале/конце)
   - `BRIDE_NAME` = `Мария` (без пробелов в начале/конце)
   - `PORT` = `10000` (без пробелов)
   - `WEBAPP_URL` = `https://wedding-bot-r0y0.onrender.com` (без пробелов)

## 🔄 После исправления:

Render автоматически перезапустит сервис. Подождите 1-2 минуты и проверьте логи.

## ✅ Проверка:

После исправления в логах должно быть:
```
✅ База данных инициализирована
✅ Бот инициализирован
🌐 Веб-сервер запущен на порту 10000
🚀 Все сервисы запущены!
```

Если ошибка повторяется, проверьте логи на Render для деталей.

