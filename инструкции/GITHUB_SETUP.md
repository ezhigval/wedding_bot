# 🔗 Подключение к GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name:** `wedding-bot` (или любое другое имя)
   - **Description:** `Telegram бот и Mini App для свадебных приглашений`
   - **Visibility:** Выберите **Private** (чтобы токен не был публичным)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже все есть)
4. Нажмите **"Create repository"**

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
cd /Users/valentinezov/Projects/Wedding_bot

# Добавьте remote (замените YOUR_USERNAME на ваш username на GitHub)
git remote add origin https://github.com/YOUR_USERNAME/wedding-bot.git

# Загрузите код на GitHub
git push -u origin main
```

**Или если используете SSH:**
```bash
git remote add origin git@github.com:YOUR_USERNAME/wedding-bot.git
git push -u origin main
```

## Шаг 3: Проверьте

Зайдите на GitHub и проверьте, что все файлы загружены.

## ⚠️ Важно!

- ✅ Файл `.env` **НЕ** будет загружен (он в .gitignore)
- ✅ Токен бота останется только у вас локально
- ✅ На сервере (Railway/Render) нужно будет добавить переменные окружения вручную

## 🔄 Обновление кода

После изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## 📝 Что уже готово:

- ✅ Git репозиторий инициализирован
- ✅ Первый коммит сделан
- ✅ .gitignore настроен правильно
- ✅ Все файлы готовы к загрузке

Теперь просто создайте репозиторий на GitHub и выполните команды из Шага 2!

