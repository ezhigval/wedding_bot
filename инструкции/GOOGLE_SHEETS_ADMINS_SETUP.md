# 🔧 Настройка Google Sheets для управления админами

## Шаг 1: Создание Service Account

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Sheets API**:
   - Перейдите в "APIs & Services" → "Library"
   - Найдите "Google Sheets API"
   - Нажмите "Enable"

4. Создайте Service Account:
   - Перейдите в "APIs & Services" → "Credentials"
   - Нажмите "Create Credentials" → "Service Account"
   - Заполните имя (например: "wedding-bot-service")
   - Нажмите "Create and Continue"
   - Роль можно оставить пустой или выбрать "Editor"
   - Нажмите "Done"

5. Создайте ключ для Service Account:
   - Найдите созданный Service Account в списке
   - Нажмите на него
   - Перейдите во вкладку "Keys"
   - Нажмите "Add Key" → "Create new key"
   - Выберите формат "JSON"
   - Нажмите "Create"
   - JSON файл скачается автоматически

## Шаг 2: Предоставление доступа к таблице

1. Откройте вашу Google таблицу: https://docs.google.com/spreadsheets/d/15-S90u4kI97Kp1NRNhyyA_cuFriUwWAgmGEa80zZ5EI/edit

2. Нажмите кнопку **"Настройки доступа"** (Share) в правом верхнем углу

3. В поле "Добавить людей и группы" вставьте **email вашего Service Account**
   - Email можно найти в JSON файле в поле `"client_email"`
   - Выглядит примерно так: `wedding-bot-service@your-project.iam.gserviceaccount.com`

4. Установите права доступа: **"Редактор"** (Editor)

5. Нажмите "Отправить"

## Шаг 3: Настройка переменной окружения

1. Откройте скачанный JSON файл с credentials

2. Скопируйте **весь** JSON (от `{` до `}`)

3. На Render:
   - Перейдите в настройки вашего сервиса
   - Найдите переменную окружения `GOOGLE_SHEETS_CREDENTIALS`
   - Вставьте весь JSON как значение (в одну строку)
   - Сохраните

**Важно:** JSON должен быть в одну строку, без переносов!

Пример правильного формата:
```
{"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}
```

## Шаг 4: Создание вкладки "Админ бота"

1. В вашей Google таблице создайте новую вкладку
2. Назовите её точно: **"Админ бота"**
3. В столбце A добавьте заголовок (опционально): `username` или `Username`
4. В столбце B добавьте заголовок (опционально): `user_id` или `User ID`
5. В столбце A добавьте username админов (без @):
   - `ezhigval`
   - `mrfilmpro`
   - и т.д.

## Шаг 5: Проверка работы

1. После деплоя на Render админ должен написать боту `/set_me_admins`
2. Бот проверит, есть ли username в столбце A
3. Если есть - сохранит `user_id` в столбец B
4. После этого админ сможет использовать `/admin`

## 🔍 Диагностика проблем

### Проблема: "Вкладка 'Админ бота' не найдена"

**Решение:**
- Убедитесь, что вкладка называется точно **"Админ бота"** (с заглавной А)
- Проверьте логи на Render - там будет список всех доступных вкладок

### Проблема: "Ошибка открытия таблицы"

**Решение:**
- Проверьте, что `GOOGLE_SHEETS_ID` правильный
- Убедитесь, что Service Account имеет доступ к таблице
- Проверьте, что email Service Account добавлен в список редакторов таблицы

### Проблема: "Ошибка обновления ячейки" или "Ошибка добавления строки"

**Решение:**
- Убедитесь, что Service Account имеет права **"Редактор"** (Editor), а не только "Читатель" (Viewer)
- Проверьте, что таблица не защищена от редактирования
- Убедитесь, что вкладка "Админ бота" не защищена

### Проблема: Данные не сохраняются

**Решение:**
- Проверьте логи на Render - там будет детальная информация об ошибках
- Убедитесь, что `GOOGLE_SHEETS_CREDENTIALS` правильно настроен (весь JSON в одну строку)
- Проверьте, что Service Account email добавлен в таблицу с правами "Редактор"

## 📋 Структура вкладки "Админ бота"

| Столбец A (username) | Столбец B (user_id) |
|---------------------|-------------------|
| ezhigval             | 1034074077        |
| mrfilmpro            | (заполнится автоматически) |

**Важно:**
- Username указывается **без @**
- User ID заполняется автоматически при вызове `/set_me_admins`
- Если username уже есть в таблице, user_id обновится в той же строке

