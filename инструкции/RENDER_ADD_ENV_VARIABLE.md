# 📝 Как добавить GOOGLE_SHEETS_CREDENTIALS на Render

## Шаг 1: Откройте ваш сервис на Render

1. Перейдите на [Render Dashboard](https://dashboard.render.com)
2. Войдите в свой аккаунт
3. В списке **Services** найдите ваш сервис с ботом (например, `wedding-bot`)
4. Нажмите на название сервиса, чтобы открыть его настройки

## Шаг 2: Найдите раздел Environment

1. В меню слева найдите раздел **Environment** (или **Environment Variables**)
2. Или прокрутите страницу вниз до раздела **Environment Variables**

Вы увидите список всех переменных окружения, которые уже установлены.

## Шаг 3: Проверьте, есть ли GOOGLE_SHEETS_CREDENTIALS

1. Прокрутите список переменных окружения
2. Найдите переменную `GOOGLE_SHEETS_CREDENTIALS`

**Если переменная ЕСТЬ:**
- Вы увидите её в списке
- Значение будет скрыто (показано как `••••••••`)
- Вы можете нажать на неё, чтобы отредактировать

**Если переменной НЕТ:**
- Вы не увидите `GOOGLE_SHEETS_CREDENTIALS` в списке
- Нужно добавить её (см. Шаг 4)

## Шаг 4: Добавьте переменную (если её нет)

1. Нажмите кнопку **"Add Environment Variable"** (или **"Add Variable"**)
2. В поле **Key** введите: `GOOGLE_SHEETS_CREDENTIALS`
3. В поле **Value** вставьте ваш JSON credentials (см. ниже)
4. Нажмите **"Save Changes"** (или **"Save"**)

**Важно:** JSON должен быть в одну строку, без переносов!

## Шаг 5: Получите JSON credentials

Если у вас еще нет JSON файла с credentials, следуйте инструкции в `GOOGLE_SHEETS_ADMINS_SETUP.md`.

Если у вас уже есть JSON файл:

1. Откройте JSON файл в текстовом редакторе
2. **Удалите все переносы строк** - JSON должен быть в одну строку
3. Скопируйте весь JSON (от `{` до `}`)

Пример правильного формата (в одну строку):
```
{"type":"service_account","project_id":"your-project","private_key_id":"abc123","private_key":"-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n","client_email":"wedding-bot@your-project.iam.gserviceaccount.com","client_id":"123456789","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"https://www.googleapis.com/robot/v1/metadata/x509/wedding-bot%40your-project.iam.gserviceaccount.com"}
```

## Шаг 6: Вставьте JSON в Render

1. В поле **Value** вставьте скопированный JSON (весь в одну строку)
2. Убедитесь, что нет переносов строк
3. Нажмите **"Save Changes"**

## Шаг 7: Перезапустите сервис

После сохранения переменной:

1. Render автоматически перезапустит сервис
2. Или нажмите **"Manual Deploy"** → **"Deploy latest commit"**
3. Подождите 1-2 минуты, пока сервис перезапустится

## Шаг 8: Проверьте логи

После перезапуска проверьте логи:

1. В Render откройте вкладку **Logs**
2. Найдите строки:
   ```
   INFO:google_sheets:Клиент Google Sheets создан успешно
   ```
   или
   ```
   WARNING:google_sheets:GOOGLE_SHEETS_CREDENTIALS не установлен
   ```

**Если видите WARNING:**
- Переменная не была сохранена правильно
- Проверьте, что JSON в одну строку
- Убедитесь, что нет лишних пробелов или кавычек

## Визуальная инструкция (текстовое описание)

```
Render Dashboard
├── Services
│   └── [Ваш сервис] ← Нажмите сюда
│       ├── Settings
│       ├── Environment ← Нажмите сюда
│       │   ├── [Список переменных]
│       │   │   ├── BOT_TOKEN
│       │   │   ├── WEBAPP_URL
│       │   │   └── ... (другие переменные)
│       │   └── [+ Add Environment Variable] ← Нажмите сюда
│       │       ├── Key: GOOGLE_SHEETS_CREDENTIALS
│       │       ├── Value: [вставьте JSON]
│       │       └── [Save Changes]
│       └── Logs ← Проверьте здесь после перезапуска
```

## Частые ошибки

### Ошибка 1: "Invalid JSON"

**Причина:** JSON содержит переносы строк или неправильный формат

**Решение:**
- Убедитесь, что JSON в одну строку
- Проверьте, что все кавычки правильные (двойные `"`, не одинарные `'`)
- Убедитесь, что JSON начинается с `{` и заканчивается `}`

### Ошибка 2: Переменная не сохраняется

**Причина:** Проблемы с интерфейсом Render

**Решение:**
1. Попробуйте сохранить еще раз
2. Обновите страницу и проверьте, появилась ли переменная
3. Попробуйте добавить через API (если доступно)

### Ошибка 3: "Credentials not found" в логах

**Причина:** Переменная не была правильно сохранена или сервис не перезапустился

**Решение:**
1. Проверьте, что переменная есть в списке
2. Перезапустите сервис вручную: **Manual Deploy** → **Deploy latest commit**
3. Проверьте логи после перезапуска

## Альтернативный способ: через файл .env (не рекомендуется для Render)

На Render лучше использовать переменные окружения через интерфейс, а не через файл `.env`, так как:
- Файлы могут не сохраняться между деплоями
- Переменные окружения более надежны
- Render автоматически перезапускает сервис при изменении переменных

## Проверка после настройки

После добавления переменной и перезапуска:

1. Напишите боту `/set_me_admins`
2. Проверьте логи - должно быть:
   ```
   INFO:google_sheets:Клиент Google Sheets создан успешно
   INFO:google_sheets:✅ Админ @ezhigval добавлен в Google Sheets
   ```
3. Проверьте Google таблицу - в столбце B должен появиться ваш `user_id`

## Нужна помощь?

Если переменная не работает:
1. Проверьте логи на Render
2. Убедитесь, что JSON правильный (можно проверить на [jsonlint.com](https://jsonlint.com))
3. Убедитесь, что Service Account имеет доступ к таблице (см. `GOOGLE_SHEETS_ADMINS_SETUP.md`)

