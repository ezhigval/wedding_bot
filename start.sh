#!/bin/bash
set -e

echo "🚀 Запуск свадебного бота..."

# Проверка переменных окружения
if [ -z "$BOT_TOKEN" ]; then
    echo "❌ ОШИБКА: BOT_TOKEN не установлен!"
    exit 1
fi

if [ -z "$GOOGLE_SHEETS_ID" ]; then
    echo "❌ ОШИБКА: GOOGLE_SHEETS_ID не установлен!"
    exit 1
fi

# Установка порта (по умолчанию 10000 для Render)
export PORT=${PORT:-10000}

echo "✅ Переменные окружения проверены"
echo "🌐 Порт: $PORT"

# Запуск Python приложения
echo "🤖 Запуск бота и веб-сервера..."
exec python server.py

