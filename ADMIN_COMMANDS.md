# Команды-действия для админов

Бот может выполнять команды-действия, но **только от админов**. Команды распознаются через LLM и выполняются автоматически.

## Доступные команды

### 1. Рассылка сообщения в группу

**Описание:** Отправляет сообщение в группу от имени группы.

**Варианты команд:**
- `Сделай рассылку всем в группу "Ваше сообщение"`
- `Отправить сообщение: Ваше сообщение`
- `Рассылка: Ваше сообщение`
- `Написать в группу "Ваше сообщение"`

**Примеры:**
```
Админ: Сделай рассылку всем в группу "Дорогие друзья, на свадьбе не будет салютов, но будет файр шоу"
Бот: ✅ Сообщение отправлено в группу:
     Дорогие друзья, на свадьбе не будет салютов, но будет файр шоу
```

### 2. Список гостей

**Описание:** Получает список зарегистрированных гостей из Google Sheets.

**Варианты команд:**
- `Пришли список гостей`
- `Покажи список гостей`
- `Выведи список участников`
- `Гости`

**Пример:**
```
Админ: Пришли список гостей
Бот: 📋 Список гостей (25):
     1. Иван Иванов
     2. Мария Петрова
     ...
```

### 3. Статистика

**Описание:** Получает статистику по зарегистрированным гостям.

**Варианты команд:**
- `Покажи статистику`
- `Статистика гостей`
- `Сколько гостей`

**Пример:**
```
Админ: Покажи статистику
Бот: 📊 Статистика:
     Зарегистрировано гостей: 25
```

### 4. Новое правило для ИИ

**Описание:** Добавляет новое правило для ИИ в Google Sheets (вкладка "Правила ИИ"). Правило будет использоваться ботом при ответах на вопросы гостей.

**Варианты команд:**
- `Напиши новое правило "Текст правила"`
- `Добавь правило: Текст правила`
- `Новое правило для ИИ: Текст правила`
- `Запиши правило "Текст правила"`

**Пример:**
```
Админ: Напиши новое правило "Если спрашивают про парковку, говори что есть бесплатная парковка на 50 мест"
Бот: ✅ Правило успешно сохранено в Google Sheets!
     Правило:
     Если спрашивают про парковку, говори что есть бесплатная парковка на 50 мест
```

**Важно:**
- Правила сохраняются в Google Sheets во вкладку "Правила ИИ"
- Если вкладка не существует, она будет создана автоматически
- Правила загружаются автоматически при каждом ответе бота
- Правила применяются ко всем ответам бота в группе

## Как это работает

1. **Распознавание:** Бот использует LLM для распознавания команд в сообщениях админов
2. **Извлечение параметров:** LLM извлекает параметры команды (например, текст сообщения для рассылки)
3. **Выполнение:** Бот выполняет действие
4. **Ответ:** Бот отвечает админу о результате выполнения

## Важные моменты

- ✅ Команды работают **только для админов**
- ✅ Команды можно писать в **любом формате** (LLM понимает естественный язык)
- ✅ Текст сообщения для рассылки можно указывать в кавычках или без них
- ✅ Бот отвечает админу о результате выполнения команды

## Тестирование

Запустите тестовый скрипт:

```bash
source venv/bin/activate
python test_admin_commands.py
```

## Добавление новых команд

Для добавления новой команды отредактируйте `admin_commands.py`:

1. Добавьте команду в `AVAILABLE_COMMANDS`:
```python
"название_команды": {
    "keywords": ["ключевое слово 1", "ключевое слово 2"],
    "description": "Описание команды",
    "action": "имя_действия"
}
```

2. Добавьте обработку в функцию `execute_command()`:
```python
elif action == "имя_действия":
    # Ваш код выполнения команды
    return "Результат выполнения"
```

