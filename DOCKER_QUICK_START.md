# 🚀 Быстрый старт: Docker деплой на Render.com

## Шаг 1: Подготовка репозитория

```bash
# Убедитесь, что все файлы на месте
git status

# Добавьте новые файлы
git add Dockerfile start.sh .dockerignore DOCKER_DEPLOYMENT.md

# Закоммитьте
git commit -m "Add Docker support for Ollama deployment"

# Запушьте в репозиторий
git push origin main
```

## Шаг 2: Создание сервиса на Render

1. **Зайдите на [dashboard.render.com](https://dashboard.render.com)**

2. **Нажмите "New +" → "Web Service"**

3. **Подключите GitHub репозиторий:**
   - Выберите ваш репозиторий
   - Выберите ветку `main` (или `dev` если деплоите из dev)

4. **Настройте сервис:**
   - **Name:** `wedding-bot` (или любое другое имя)
   - **Environment:** `Docker` ⚠️ ВАЖНО: выберите Docker, не Python!
   - **Region:** Выберите ближайший регион
   - **Branch:** `main` (или ваша ветка)
   - **Root Directory:** оставьте пустым (`.`)
   - **Dockerfile Path:** оставьте пустым (автоматически найдет `Dockerfile`)
   - **Docker Context:** оставьте пустым (`.`)

5. **Выберите план:**
   - ⚠️ **ОБЯЗАТЕЛЬНО:** Standard план или выше
   - Free/Starter планы НЕ подойдут (недостаточно RAM для Ollama)
   - Standard план: $7/месяц, 2GB RAM - минимум для работы

6. **Нажмите "Create Web Service"**

## Шаг 3: Настройка переменных окружения

После создания сервиса, в разделе **"Environment"** добавьте переменные:

### Обязательные переменные:

```bash
# Telegram Bot
BOT_TOKEN=ваш_токен_бота

# Свадьба
WEDDING_DATE=2026-06-05
GROOM_NAME=Валентин
BRIDE_NAME=Мария
WEDDING_ADDRESS=Разъезжая улица, 15, городской посёлок Токсово, Токсовское городское поселение, Всеволожский район, Ленинградская область

# Mini App (замените на ваш URL после деплоя)
WEBAPP_URL=https://wedding-bot-xxxx.onrender.com

# Группа
GROUP_LINK=https://t.me/+your_group_link
GROUP_ID=your_group_id

# Админы
ADMINS=@username1,@username2

# Google Sheets
GOOGLE_SHEETS_ID=your_google_sheets_id
GOOGLE_SHEETS_CREDENTIALS={"type":"service_account",...}

# Ollama (для Docker варианта можно не указывать, будет localhost)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b
```

### Опциональные:

```bash
GROOM_TELEGRAM=ezhigval
BRIDE_TELEGRAM=mrfilmpro
ADMIN_USER_ID=your_user_id
```

## Шаг 4: Первый деплой

1. **После настройки переменных, Render автоматически начнет деплой**

2. **Первый деплой займет 15-20 минут:**
   - Сборка Docker образа: ~5 минут
   - Установка Ollama: ~2 минуты
   - Скачивание модели qwen2.5:7b: ~10-15 минут (зависит от скорости)

3. **Следите за логами:**
   - В разделе "Logs" вы увидите процесс
   - Ожидайте сообщения:
     ```
     🚀 Запуск свадебного бота с Ollama...
     📦 Запуск Ollama сервера...
     ✅ Ollama запущен!
     📥 Скачивание модели qwen2.5:7b...
     ✅ Модель загружена!
     🤖 Запуск бота...
     ```

## Шаг 5: Проверка работы

### 1. Проверьте логи

В разделе "Logs" должны быть:
```
✅ Ollama запущен!
✅ Модель загружена!
🚀 Все сервисы запущены!
```

### 2. Проверьте сайт

Откройте `https://your-app-name.onrender.com` - должен загрузиться сайт приглашения

### 3. Проверьте бота

Отправьте `/start` боту - должен прийти ответ с приглашением

### 4. Проверьте LLM

Напишите в группу `@бот когда свадьба?` - бот должен ответить через ИИ

## ⚠️ Важные моменты

### Память и ресурсы

- **qwen2.5:7b** требует ~4-8GB RAM
- На Standard плане (2GB) может быть тесно
- Если возникают проблемы с памятью, используйте более легкую модель:

```bash
OLLAMA_MODEL=llama3.2:3b  # Требует ~2GB RAM
```

### Обновление WEBAPP_URL

После первого деплоя:
1. Скопируйте URL вашего сервиса (например: `https://wedding-bot-xxxx.onrender.com`)
2. Обновите переменную `WEBAPP_URL` в настройках Render
3. Перезапустите сервис (Manual Deploy → Clear build cache & deploy)

### Мониторинг

- Регулярно проверяйте логи на наличие ошибок
- Следите за использованием памяти (в разделе Metrics)
- Если сервис падает - проверьте логи

## 🔧 Устранение проблем

### Сервис не запускается

1. Проверьте логи на ошибки
2. Убедитесь, что выбран Standard план
3. Проверьте все переменные окружения

### Ollama не запускается

1. Проверьте логи - должны быть сообщения о запуске Ollama
2. Убедитесь, что порт 11434 доступен
3. Проверьте использование памяти

### Модель не скачивается

1. Первая загрузка может занять 20-30 минут
2. Проверьте интернет-соединение на сервере
3. Если долго - попробуйте более легкую модель

### Недостаточно памяти

**Симптомы:** Сервис падает, ошибки OOM

**Решение:**
```bash
# Используйте более легкую модель
OLLAMA_MODEL=llama3.2:3b
```

## 📊 Оптимизация

### Использование более легкой модели

Если Standard план не справляется:

1. Измените переменную:
```bash
OLLAMA_MODEL=llama3.2:3b
```

2. Перезапустите сервис

### Предзагрузка модели (опционально)

Чтобы ускорить последующие деплои, можно предзагрузить модель в Dockerfile, но это увеличит размер образа.

## ✅ Чек-лист

- [ ] Все файлы закоммичены и запушены
- [ ] Создан Web Service на Render
- [ ] Выбран **Docker** как Environment
- [ ] Выбран **Standard план** или выше
- [ ] Все переменные окружения настроены
- [ ] Первый деплой завершен
- [ ] Логи проверены - все работает
- [ ] Сайт открывается
- [ ] Бот отвечает
- [ ] LLM работает в группе

## 🎉 Готово!

После успешного деплоя ваш бот будет работать с полной поддержкой ИИ прямо на Render!

