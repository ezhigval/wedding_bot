# Деплой с Ollama через Docker на Render.com

## ⚠️ Важные ограничения

**Render.com имеет ограничения:**
- Бесплатный план: 512MB RAM (недостаточно для Ollama)
- Стартер план: 512MB RAM (тоже может быть недостаточно)
- Standard план: 2GB+ RAM (может работать с легкими моделями)

**Ollama с моделью qwen2.5:7b требует:**
- ~4-8GB RAM
- CPU с поддержкой AVX2

**Рекомендация:** Используйте **Standard план** или выше для работы с Ollama.

## 🐳 Вариант 1: Docker на Render (рекомендуется)

### Шаг 1: Подготовка

1. Убедитесь, что у вас есть Dockerfile и start.sh
2. Закоммитьте все изменения:
```bash
git add Dockerfile start.sh .dockerignore
git commit -m "Add Docker support for Ollama"
git push origin main
```

### Шаг 2: Создание сервиса на Render

1. Зайдите на [dashboard.render.com](https://dashboard.render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите GitHub репозиторий
4. В настройках:
   - **Environment:** `Docker`
   - **Dockerfile Path:** `Dockerfile` (или оставьте пустым)
   - **Docker Context:** `.` (корень проекта)
   - **Plan:** **Standard** или выше (обязательно!)

### Шаг 3: Переменные окружения

Добавьте те же переменные, что и в обычном деплое, но:
- `OLLAMA_BASE_URL` можно не указывать (будет использоваться localhost)
- Или укажите `OLLAMA_BASE_URL=http://localhost:11434`

### Шаг 4: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь сборки Docker образа (может занять 10-15 минут)
3. Проверьте логи

## 🔧 Вариант 2: Гибридное решение (Ollama локально + приложение на Render)

Если Docker не работает или недостаточно ресурсов:

### На Render (приложение):
- Обычный Python сервис
- `OLLAMA_BASE_URL` указывает на внешний сервер

### Отдельно (Ollama):
- VPS с Ollama
- Или домашний компьютер + ngrok
- Или облачный сервис

## 📊 Сравнение вариантов

| Вариант | Сложность | Стоимость | Надежность | Рекомендация |
|---------|-----------|-----------|------------|--------------|
| Docker на Render | Средняя | $$$ | ⭐⭐⭐ | Если есть Standard план |
| Внешний Ollama | Низкая | $ | ⭐⭐⭐⭐ | Рекомендуется |
| Без LLM | Низкая | $ | ⭐⭐⭐⭐⭐ | Если не нужен ИИ |

## 🚀 Быстрый старт с Docker

1. **Подготовьте репозиторий:**
```bash
git add Dockerfile start.sh .dockerignore
git commit -m "Add Docker support"
git push origin main
```

2. **Создайте сервис на Render:**
   - Выберите **Docker** как Environment
   - Выберите **Standard план** или выше
   - Настройте переменные окружения

3. **Дождитесь деплоя:**
   - Первый деплой займет 10-15 минут (скачивание модели)
   - Последующие деплои быстрее

## ⚙️ Настройка модели

По умолчанию используется `qwen2.5:7b`. Чтобы изменить:

1. Отредактируйте `start.sh`:
```bash
# Замените qwen2.5:7b на нужную модель
ollama pull llama3.2:3b  # Более легкая модель
```

2. Или через переменную окружения:
```bash
OLLAMA_MODEL=llama3.2:3b
```

## 🔍 Проверка работы

После деплоя проверьте логи:

1. **Ollama запущен:**
```
✅ Ollama запущен!
```

2. **Модель загружена:**
```
✅ Модель уже установлена!
```

3. **Бот запущен:**
```
🚀 Все сервисы запущены!
```

## 🐛 Устранение проблем

### Недостаточно памяти

**Симптомы:** Сервис падает, ошибки OOM (Out of Memory)

**Решение:**
- Используйте более легкую модель (llama3.2:3b)
- Или перейдите на более высокий план Render
- Или используйте внешний Ollama сервер

### Ollama не запускается

**Симптомы:** Ошибки в логах, бот не отвечает на LLM запросы

**Решение:**
1. Проверьте логи на наличие ошибок
2. Убедитесь, что порт 11434 доступен
3. Проверьте, что модель скачалась

### Модель не скачивается

**Симптомы:** Долгая загрузка, таймауты

**Решение:**
- Первая загрузка может занять 20-30 минут
- Убедитесь, что есть стабильное интернет-соединение
- Рассмотрите предварительную загрузку модели в образ

## 💡 Оптимизация

### Предзагрузка модели в образ

Чтобы ускорить деплой, можно предзагрузить модель в Dockerfile:

```dockerfile
# После установки Ollama
RUN ollama pull qwen2.5:7b
```

⚠️ Это увеличит размер образа, но ускорит первый запуск.

### Использование более легкой модели

Для экономии ресурсов используйте `llama3.2:3b`:
- Требует ~2GB RAM
- Быстрее отвечает
- Меньше качество, но все еще хорошо для чата

## 📝 Чек-лист

- [ ] Dockerfile создан
- [ ] start.sh создан и исполняемый
- [ ] .dockerignore настроен
- [ ] Выбран Standard план или выше на Render
- [ ] Переменные окружения настроены
- [ ] Репозиторий закоммичен и запушен
- [ ] Деплой запущен
- [ ] Логи проверены

## 🆘 Альтернативы

Если Docker на Render не работает:

1. **Railway.app** - поддерживает Docker и GPU
2. **Fly.io** - хорошая поддержка Docker
3. **DigitalOcean App Platform** - Docker + больше ресурсов
4. **VPS (Hetzner, DigitalOcean)** - полный контроль

