# Тестирование бота в групповом чате

## Быстрый тест (локально)

Запустите тестовый скрипт:

```bash
source venv/bin/activate
python test_group_bot.py
```

Скрипт проверит:
- ✅ Прямые упоминания через @username
- ✅ Слово "бот" в сообщении
- ✅ Понимание контекста (вопрос от одного, "бот" от другого)
- ✅ Разные варианты обращений
- ✅ Поиск вопросов в контексте
- ✅ Интеграцию с LLM (если Ollama запущен)

## Тестирование в реальной группе Telegram

### 1. Подготовка

Убедитесь, что:
- ✅ Ollama запущен: `ollama serve`
- ✅ Модель установлена: `ollama pull qwen2.5:7b`
- ✅ Бот запущен: `python server.py` (или через `dev_server.py` для локальной разработки)

### 2. Добавление бота в группу

1. Создайте тестовую группу в Telegram
2. Добавьте бота в группу
3. Дайте боту права администратора (опционально, но рекомендуется)
4. Убедитесь, что `GROUP_ID` в `.env` соответствует ID вашей группы

### 3. Сценарии для тестирования

#### Сценарий 1: Прямое упоминание
```
Вы: @ваш_бот когда свадьба?
Бот: должен ответить
```

#### Сценарий 2: Слово "бот"
```
Вы: бот, подскажи где будет свадьба?
Бот: должен ответить
```

#### Сценарий 3: Контекст (вопрос от одного, "бот" от другого)
```
Гость1: Когда начинается свадьба?
Админ: бот
Бот: должен найти вопрос Гостя1 и ответить на него
```

#### Сценарий 4: Обычное сообщение (бот НЕ должен отвечать)
```
Вы: Привет всем!
Бот: НЕ должен отвечать
```

#### Сценарий 5: Вопрос без обращения (бот НЕ должен отвечать)
```
Вы: Когда свадьба?
Бот: НЕ должен отвечать (нет обращения)
```

### 4. Проверка логов

Следите за логами бота, чтобы увидеть:
- Когда бот получает сообщение
- Определяет ли он, что нужно ответить
- Находит ли вопрос в контексте
- Что отправляет в LLM
- Какой ответ получил от LLM

### 5. Отладка

Если что-то не работает:

1. **Бот не отвечает на упоминания:**
   - Проверьте `bot.username` в логах
   - Убедитесь, что бот правильно определяет username

2. **Контекст не работает:**
   - Проверьте, что сообщения сохраняются в `group_context.py`
   - Убедитесь, что временное окно (2 минуты) не истекло

3. **LLM не отвечает:**
   - Проверьте, что Ollama запущен: `curl http://localhost:11434/api/tags`
   - Проверьте логи на наличие ошибок

## Примеры тестовых диалогов

### Диалог 1: Простой вопрос
```
Гость: @бот когда свадьба?
Бот: Свадьба состоится 5 июня 2026 года! 🎉
```

### Диалог 2: Контекстный вопрос
```
Гость1: Где будет свадьба?
Админ: бот
Бот: Свадьба пройдет в Панорама Холл, Токсово. Адрес: Разъезжая улица, 15. 📍
```

### Диалог 3: Множественные вопросы
```
Гость1: Когда свадьба?
Гость2: Где будет?
Админ: бот
Бот: (должен ответить на последний вопрос от Гостя2)
```

## Полезные команды

```bash
# Проверить доступность Ollama
curl http://localhost:11434/api/tags

# Запустить тесты
python test_group_bot.py

# Запустить бота локально (только веб-сервер)
python dev_server.py

# Запустить полный бот
python server.py
```

