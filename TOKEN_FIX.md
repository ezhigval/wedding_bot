# 🔧 Исправление ошибки токена на Render

## ❌ Ошибка:
```
TokenValidationError: Token is invalid! It can't contains spaces.
```

## ✅ Решение:

### Проблема:
Токен бота на Render содержит пробелы или кавычки.

### Как исправить на Render:

1. **Зайдите на Render:**
   - Откройте ваш сервис `wedding-bot`
   - Перейдите в **Environment** (в левом меню)

2. **Найдите переменную BOT_TOKEN:**
   - Найдите строку с `BOT_TOKEN`
   - Нажмите на неё для редактирования

3. **ВАЖНО - правильное значение (скопируйте ТОЧНО):**
   ```
   8295772557:AAH_udSU1FyjYH-mekFDZht2Yuy0EKI7s5g
   ```

4. **Проверьте:**
   - ❌ НЕТ пробелов в начале
   - ❌ НЕТ пробелов в конце
   - ❌ НЕТ кавычек (ни `"`, ни `'`)
   - ❌ НЕТ переносов строк
   - ✅ Только сам токен

5. **Если токен в кавычках:**
   - Удалите кавычки полностью
   - Должно быть: `8295772557:AAH_udSU1FyjYH-mekFDZht2Yuy0EKI7s5g`
   - НЕ: `"8295772557:AAH_udSU1FyjYH-mekFDZht2Yuy0EKI7s5g"`

6. **Сохраните:**
   - Нажмите **Save Changes**
   - Render автоматически перезапустит сервис

### Проверка других переменных:

Убедитесь, что все переменные БЕЗ пробелов в начале/конце:
- `WEDDING_DATE` = `2026-06-06`
- `GROOM_NAME` = `Валентин`
- `BRIDE_NAME` = `Мария`
- `PORT` = `10000`
- `WEBAPP_URL` = `https://wedding-bot-r0y0.onrender.com`

### После исправления:

1. Подождите 1-2 минуты (Render перезапустит сервис)
2. Проверьте логи - должно быть:
   ```
   ✅ База данных инициализирована
   ✅ Бот инициализирован
   🌐 Веб-сервер запущен на порту 10000
   🚀 Все сервисы запущены!
   ```

### Если ошибка повторяется:

1. Удалите переменную `BOT_TOKEN` полностью
2. Добавьте её заново
3. Вставьте токен БЕЗ кавычек и пробелов
4. Сохраните

## 📝 Правильный формат на Render:

В поле "Value" должно быть ТОЛЬКО:
```
8295772557:AAH_udSU1FyjYH-mekFDZht2Yuy0EKI7s5g
```

БЕЗ:
- Кавычек
- Пробелов
- Переносов строк
- Дополнительных символов

